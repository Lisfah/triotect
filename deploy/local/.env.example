# TrioTect Local Environment Variables
# Copy this file to .env and fill in secrets before running.
# ─────────────────────────────────────────────────────────────
# 🟢 CONFIG DATA (preserved across resets)
# 🔴 TRANSACTIONAL DATA (cleared by scripts/reset.sh)

# ── JWT (shared secret across Identity Provider + Order Gateway) ──────────────
# 🟢 CONFIG
JWT_SECRET_KEY=CHANGE_ME_AT_LEAST_32_RANDOM_CHARS_HERE
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=30
JWT_REFRESH_TOKEN_EXPIRE_DAYS=7

# ── Identity Provider PostgreSQL ──────────────────────────────────────────────
# 🟢 CONFIG
IDENTITY_DB_NAME=identity_db
IDENTITY_DB_USER=identity_user
IDENTITY_DB_PASSWORD=CHANGE_ME_IDENTITY_DB_PASS

# ── Stock Service PostgreSQL ──────────────────────────────────────────────────
# 🟢 CONFIG
STOCK_DB_NAME=stock_db
STOCK_DB_USER=stock_user
STOCK_DB_PASSWORD=CHANGE_ME_STOCK_DB_PASS

# ── Kitchen Queue PostgreSQL ──────────────────────────────────────────────────
# 🟢 CONFIG
KITCHEN_DB_NAME=kitchen_db
KITCHEN_DB_USER=kitchen_user
KITCHEN_DB_PASSWORD=CHANGE_ME_KITCHEN_DB_PASS

# ── Redis ─────────────────────────────────────────────────────────────────────
# 🟢 CONFIG
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# ── Identity Provider Settings ────────────────────────────────────────────────
# 🟢 CONFIG
RATE_LIMIT_MAX_ATTEMPTS=3
RATE_LIMIT_WINDOW_SECONDS=60

# ── Order Gateway Settings ────────────────────────────────────────────────────
# 🟢 CONFIG
STOCK_CACHE_TTL_SECONDS=10
IDEMPOTENCY_KEY_TTL_SECONDS=86400

# ── Stock Service Settings ────────────────────────────────────────────────────
# 🟢 CONFIG
OPT_LOCK_MAX_RETRIES=5
OPT_LOCK_BASE_DELAY_MS=50
OPT_LOCK_MAX_DELAY_MS=1000
OPT_LOCK_JITTER_MS=50

# ── Kitchen Queue Settings ────────────────────────────────────────────────────
# 🟢 CONFIG
KITCHEN_MIN_PREP_SECONDS=3
KITCHEN_MAX_PREP_SECONDS=7

# ── Notification Hub ──────────────────────────────────────────────────────────
# 🟢 CONFIG
SSE_KEEPALIVE_INTERVAL_SECONDS=15
SSE_RETRY_MILLISECONDS=3000

# ── Grafana ───────────────────────────────────────────────────────────────────
# 🟢 CONFIG
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=CHANGE_ME_GRAFANA_PASS

# ── Debug ─────────────────────────────────────────────────────────────────────
DEBUG=false
METRICS_ENABLED=true
HEALTH_CHECK_TIMEOUT=5.0
